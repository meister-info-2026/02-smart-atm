# 학생용 설치 및 사용 매뉴얼 (스마트 금융 보안 ATM)

> 이 매뉴얼은 **스마트 금융 보안 ATM**(노약자를 위한 AI 문자 보이스피싱 예방 시스템)
> 팀에 맞게 예시를 채워 넣은 버전입니다. 이 킷의 `.agents/`(rules/skills/workflows/
> hooks/agents), 핵심 보일러플레이트 코드, 그리고 **`AGENTS.md`까지 이미 이 프로젝트에
> 맞게 준비되어 있습니다** — 1단계의 PRD 작성·AGENTS.md 완성 과정은 이미 끝나 있으니
> 내용을 확인만 하고 넘어가면 됩니다(왜 이렇게 채워졌는지는
> `docs/스마트금융보안ATM-AGENTS-초안-및-보충가이드.md` 참고).
>
> ⚠️ 이 프로젝트는 킷의 기본 흐름("웹캠이 감지 → 즉시 액추에이터 제어")과 구조가
> 다릅니다. **QR 코드 자체에 위험 판정 결과가 담겨 있고, ATM(라즈베리파이)이 그 QR을
> 읽는 즉시 스스로 판단**합니다. 그래서 아래 3~4단계는 원래 매뉴얼의 "영상인식 →
> 트리거" 순서를 이 프로젝트에 맞게 다시 짰습니다 — 반드시 순서대로 따라 하세요.
>
> 각 단계의 프롬프트/확인 방법은 이 문서를 따르고, 진행 체크만 빠르게 하고 싶다면
> `docs/학생-수작업-로드맵.md` 체크리스트를 함께 씁니다(체크리스트는 범용 버전이니
> 항목 이름만 우리 팀 것으로 바꿔서 사용합니다).

---

## 0단계. GitHub에서 받기 + 하네스 구축 (팀당 딱 한 번만)

### 0-1. 저장소 받기
팀장 또는 백엔드 담당 1명만 하면 됩니다. PowerShell에서:
```powershell
git clone -b atm [교사가 안내한 GitHub 저장소 주소] 스마트-금융-보안-ATM
cd 스마트-금융-보안-ATM
```
Git이 익숙하지 않다면, GitHub 저장소 페이지에서 **브랜치를 `atm`으로 전환한 뒤
Code → Download ZIP**으로 받아 압축을 풀고 그 폴더로 이동해도 됩니다.

> ⚠️ 반드시 **`atm` 브랜치**를 받아야 합니다. `main` 브랜치는 여러 팀이 함께 쓰는
> 범용 템플릿이라 이 팀 전용 `AGENTS.md`와 `qr-recognition-integration`,
> `callcenter-session-integration` 스킬이 들어있지 않습니다.

**확인**: `AGENTS.md`, `.agents/`, `docs/`, `backend/`, `frontend/`, `vision/`, `pi/`가 들어있으면 성공입니다 — 공통 인터페이스(`backend/iot/base.py`, `backend/websocket_manager.py`, `frontend/components/dashboard/ConnectionBadge.tsx`)와 모델 가중치(`vision/yolov8n.pt`)는
교사가 미리 완성해서 저장소에 올려둔 것이라 별도 설치 과정이 필요 없습니다(이 팀은
`yolov8n.pt`를 쓰지 않지만 그대로 둡니다). `AGENTS.md`를 열어 팀 주제가 "노약자를 위한
AI 문자 보이스피싱 예방 시스템"으로 이미 채워져 있는지도 확인합니다.

### 0-2. Antigravity로 열기
Antigravity IDE에서 방금 받은 폴더를 워크스페이스로 엽니다.

### 0-3. 하네스 구축 프롬프트 — 프로젝트 뼈대 완성
스타터 킷에 기본 구조가 준비되어 있습니다. Next.js 프론트엔드 스캐폴드와 백엔드 가상환경을 준비합니다.
> 💡 **참고**: Windows PowerShell에서 `pip freeze`를 할 때는 인코딩 충돌을 방지하기 위해 항상 UTF-8로 저장합니다:
> `pip freeze | Out-File -Encoding utf8 requirements.txt`

Antigravity 채팅창에 아래 프롬프트를 입력해 스캐폴드를 완성합니다:

```
이 프로젝트에는 이미 AGENTS.md, .agents/ 하네스, 그리고 기본 보일러플레이트(backend/iot/base.py, backend/websocket_manager.py, vision/yolov8n.pt 등)가 준비되어 있어. 
AGENTS.md의 기술 스택과 폴더 구조를 참고해서 아래를 완성해줘.

1. frontend/ — Next.js(TypeScript, Tailwind) 프로젝트 스캐폴드 (.env.example 충돌 없이 생성)
2. backend/ — Python venv를 활성화하고 requirements.txt(fastapi, uvicorn[standard], websockets, pymysql, python-dotenv, httpx 포함)의 패키지를 설치해줘
3. .gitignore — venv/, __pycache__/, *.pyc, .env, .env.local, node_modules/, .next/ 를 포함해줘
4. 각 폴더의 .env.example을 확인하고 누락된 항목이 없도록 정리해줘

AGENTS.md, .agents/, docs/ 폴더 및 사전 탑재된 유틸리티 파일은 이미 완성되어 있으니 절대 임의로 삭제하거나 덮어쓰지 마 (karpathy-principles.md의 "외과적 변경" 원칙).
```

**확인**: `frontend/`, `backend/`(안에 `venv/`, `requirements.txt`), `vision/`,
`pi/`, `.gitignore`, 그리고 4개의 `.env.example`이 생겼는지 확인합니다.

### Antigravity 인식 확인
Antigravity IDE에서 이 폴더를 워크스페이스로 엽니다. 상단 "…" 메뉴 → **Customizations** 패널에서
Rules/Skills/Workflows 목록에 방금 설치된 항목들이 보이는지 확인합니다. 안 보이면 워크스페이스를
닫았다가 다시 엽니다. Skills 목록에 `qr-recognition-integration`, `callcenter-session-integration`이
함께 보이면 이 팀 전용 스킬이 정상적으로 인식된 것입니다.

**⚠️ Rules는 3개만 "Always On"으로 설정하세요.** Customizations → Rules에서 아래
3개만 **Always On**으로 켭니다.
- `karpathy-principles` (모든 작업의 기본 전제)
- `security-rules`
- `coding-standards`

나머지 규칙(`api-rules`, `db-rules`, `hardware-rules`, `vision-rules`, `git-rules`,
`deploy-rules`, `ui-ux-rules`)은 Always On으로 켜지 않아도 됩니다 — 각 `agent.md`의
"항상 참고" 항목에 이미 연결돼 있어서, 해당 역할(예: db-agent)을 부를 때 AI가
자동으로 읽습니다. 10개를 전부 Always On으로 켜면 채팅마다 안 쓰는 규칙까지
매번 8,000~12,000 토큰을 소모하므로, 딱 이 3개만 켜는 것이 무료 티어 유지의 핵심입니다.

---

## 1단계. 팀 기획 & AGENTS.md 완성 — 이 팀은 이미 완료됨

> 💡 이 팀은 PRD 검토를 거쳐 **`AGENTS.md`가 이미 완성되어 있습니다.** 아래 1~3번
> 과정을 처음부터 할 필요 없이, `AGENTS.md`를 열어 팀원들과 함께 내용을 확인만 하면
> 됩니다. 왜 이렇게 채워졌는지 배경이 궁금하면
> `docs/스마트금융보안ATM-AGENTS-초안-및-보충가이드.md`를 참고합니다.
>
> 단, 아래 항목은 여전히 `[미정]`으로 남아있으니 팀이 직접 정해서 `AGENTS.md`에
> 채워 넣어야 합니다.
> - 팀명, 팀원 역할 분담(5인 — 팀장/시스템통합, 모바일앱, AI/백엔드, ATM/하드웨어, 콜센터·UX·테스트)
> - 현금 배출 시연 장치의 구체 종류(서보모터 / DC모터 + 드라이버)와 GPIO 핀맵
> - QR 인식 장치(카메라 또는 전용 QR 스캐너)의 구체 모델
> - `CAUTION` 등급일 때 ATM이 취할 동작(MVP는 SAFE/DANGER만 우선 구현해도 됨)
> - 콜센터 확인을 네트워크 연동으로 할지, ATM 로컬 버튼으로 갈음할지(2부 참고)
> - AI 위험 분석을 규칙 기반에서 실제 AI API/모델로 확장할지, 한다면 어떤 것으로
>
> (참고: 다른 주제로 새로 시작하는 팀이 이 문서를 참고한다면, 아래 1~3번 과정을
> `main` 브랜치의 기본 `AGENTS.md`로 그대로 따라 하면 됩니다.)

1. **팀 미팅**: `docs/팀-주제-브레인스토밍-워크시트.md`를 참고하여 팀 주제, 센서, 액추에이터, 영상인식 대상, 트리거 규칙, 4인 역할을 정합니다.
2. **PRD 작성**: 브라우저에서 ChatGPT/Claude에 팀 미팅 결과를 전달해 [PRD(제품 요구사항 정의서)]를 작성합니다.
3. **AGENTS.md 완성**: 생성된 `PRD.md` 파일과 스타터 킷의 `AGENTS.md`를 ChatGPT/Claude에 함께 업로드하여 완성된 `AGENTS.md`를 출력받아 워크스페이스에 붙여넣습니다.

### .env 파일 만들기
각 폴더의 `.env.example` 4개를 각각 `.env`로 복사한 뒤 실제 값을 채웁니다
(`.env`는 `.gitignore`에 있어 커밋되지 않습니다).
```powershell
copy backend\.env.example backend\.env
copy frontend\.env.example frontend\.env
copy vision\.env.example vision\.env
copy pi\.env.example pi\.env
```
> 💡 **보안 주의**: `backend/.env`, `vision/.env`, `pi/.env`의 `DEVICE_API_KEY`는 서로 같은 값을 가져야 인증이 동작합니다. 보안을 위해 각 팀만의 고유한 비밀 키(예: `myteam_secret_key_1234`)를 정해 세 파일에 동일하게 채워주세요.

---

## 2단계 (1주차). Mock 백엔드 + DB + 대시보드 + QR 생성

> 🖥️ **동시 실행 멀티 터미널 안내**:
> **터미널을 2개 열어 각각 실행 상태를 유지**합니다. 이 단계는 카메라·하드웨어가 전혀
> 필요 없는 순수 소프트웨어 단계입니다(QR 인식은 3단계에서 다룹니다).
> - **터미널 1**: 백엔드 API & WebSocket (`uvicorn main:app --reload --port 8000`)
> - **터미널 2**: Next.js 대시보드/사용자 화면 (`npm run dev`)

```
┌─────────────────────────┐   WebSocket / REST    ┌─────────────────────────┐
│  터미널 1: 백엔드 (8000)  │ ◄──────────────────► │ 터미널 2: 프론트엔드(3000) │
└─────────────────────────┘                       └─────────────────────────┘
```

### 2-1. 백엔드 환경 준비 (직접 실행)
```powershell
# [터미널 1]
cd backend
.\venv\Scripts\Activate.ps1
```
XAMPP를 켜서 MariaDB를 실행하고, phpMyAdmin에서 DB(`atm_security` 등 우리 팀 이름)를 만듭니다.
`backend/.env`에 DB 접속 정보를 채웁니다.

### 2-2. DB + Mock 디바이스 + API

이 팀은 desired-state 폴링 대상 디바이스가 **현금 배출 시연 장치 하나**뿐입니다 —
QR 인식 장치는 `devices` 테이블/DeviceProvider 대상이 아니라 `analysis_sessions`
단위로 처리합니다(`callcenter-session-integration` 스킬 참고). 아래 프롬프트를
순서대로 입력합니다.

```
너는 이 프로젝트의 db-agent다. .agents/skills/db-integration/SKILL.md와
.agents/skills/callcenter-session-integration/SKILL.md를 따른다.

db-rules.md의 최소 스키마로 backend/db.py와 backend/db/init.sql을 만들어줘.
init.sql에는 AGENTS.md 팀 정보의 액추에이터(현금 배출 시연 장치)를 devices 테이블
시드 데이터로 포함하고, callcenter-session-integration 스킬의 analysis_sessions
테이블도 함께 만들어줘.
```

### 2-2-1. DB 초기화 스크립트 실행 (직접 실행)
AI가 만든 `init.sql`은 파일로만 존재할 뿐, 아직 MySQL에 반영되지 않았습니다.
XAMPP MySQL이 켜진 상태에서 PowerShell로 직접 실행합니다.
```powershell
cd backend
Get-Content db\init.sql | mysql -u root
```
(mysql 명령이 PATH에 없다면, phpMyAdmin 웹 화면의 **SQL 탭**에 `init.sql` 내용을 그대로 붙여넣어 실행해도 됩니다.)

**확인**: phpMyAdmin에서 `devices` 테이블에 현금 배출 시연 장치 행이, `analysis_sessions`
테이블이 생성되어 있는지 확인합니다.

```
너는 이 프로젝트의 hardware-agent다. .agents/skills/hardware-integration/SKILL.md를 따른다.

backend/iot/base.py의 DeviceProvider를 상속받아 우리 팀 액추에이터(현금 배출 시연
장치)에 대한 MockDeviceProvider를 만들어줘. devices 테이블의 id 값과 일치시켜줘.
```

```
너는 이 프로젝트의 backend-agent다. .agents/skills/iot-endpoint-generator/SKILL.md와
.agents/skills/callcenter-session-integration/SKILL.md를 따른다.

현금 배출 시연 장치에 대한 조회/제어 엔드포인트와, callcenter-session-integration
스킬대로 POST /api/analyze(규칙 기반 키워드 판별), POST /api/v1/atm/scan,
GET /api/v1/atm/session-status/{session_id}를 backend/main.py에 완성해줘.
```

### 2-2-2. 백엔드 서버 실행 (터미널 1)
```powershell
# [터미널 1]
cd backend
uvicorn main:app --reload --port 8000
```
> 💡 `backend/main.py` 최상단에 `sys.path` 자동 추가 코드가 탑재되어 있으므로 `ModuleNotFoundError: No module named 'backend'` 에러 없이 정상 구동됩니다.

**확인**: 브라우저에서 `http://localhost:8000/docs`(Swagger) 및 `http://localhost:8000/health` 접속 확인. Swagger에서 `POST /api/analyze`에 의심 문자를 넣어 `risk_level`이 정상 반환되는지 먼저 확인합니다.

### 2-3. 대시보드 + 사용자 화면 (터미널 2)
먼저 새 터미널을 열고 필요한 패키지를 직접 설치합니다.
```powershell
# [터미널 2]
cd frontend
npm install
npm install lucide-react
```
```
너는 이 프로젝트의 frontend-agent다. .agents/rules/ui-ux-rules.md와
.agents/skills/dashboard-ui-design/SKILL.md, .agents/skills/qr-recognition-integration/SKILL.md를
따른다.

사전 제공된 components/dashboard/statusColor.ts와 ConnectionBadge.tsx를 활용해서
아래를 만들어줘.
1. 사용자용 화면: 의심 문자 입력/선택 → POST /api/analyze 호출 → 안전/의심/위험 결과와
   탐지 근거 표시 → 위험(DANGER)이면 qrcode 패키지로 QR을 생성해 화면에 표시
2. ATM 상태 대시보드: 현금 배출 시연 장치 상태를 실시간으로 보여주고, WITHDRAW_BLOCKED
   상태일 때는 AlertCard 패턴(자동으로 꺼지지 않고 수동 확인 전까지 유지)으로 표시
시간 렌더링에는 suppressHydrationWarning을 적용해줘.
```

### 2-3-1. 프론트엔드 서버 실행 (터미널 2)
```powershell
# [터미널 2]
npm run dev
```
**확인**: 브라우저에서 `http://localhost:3000` 접속 후 실시간 연결 상태 배지("● 실시간 연결됨")를 확인합니다. 의심 문자(예: "검찰입니다. 계좌가 범죄에 연루되었습니다. 현금 500만 원을 인출해 지정 장소로 가져오세요.")를 넣고 위험 판정과 QR 코드가 화면에 뜨는지 확인합니다. 아직 이 QR을 스캔할 대상은 없습니다(3단계에서 진행).

---

## 3단계 (2주차). QR 인식 PC 검증 + 콜센터 확인 흐름

이 프로젝트는 기본 킷 예시("웹캠이 감지 → 즉시 트리거")와 다릅니다. **QR 안에
이미 위험 판정 결과가 담겨 있고, ATM은 그 QR을 읽는 즉시 스스로 판단**합니다.
그래서 이 단계는 (a) QR 인식 로직이 실제로 작동하는지 PC 웹캠으로 먼저 검증하고,
(b) 위험 판정 이후 필요한 콜센터 확인 흐름을 완성하는 데 집중합니다. 최종 QR 인식
코드는 4단계에서 그대로 라즈베리파이로 옮겨집니다.

### 3-1. QR 인식 PC 사전 검증
> ⚠️ **웹캠 충돌 방지 사전 체크**: Zoom, Teams, Discord, Windows 카메라 앱 등을
> 모두 종료하세요.

새 터미널(터미널 3)에서 QR 인식만 가볍게 테스트합니다. 새 모델 다운로드나 무거운
패키지 설치가 필요 없습니다(`opencv-python`만 있으면 됩니다).
```powershell
# [터미널 3]
cd vision
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install opencv-python
```

```
너는 이 프로젝트의 hardware-agent다. .agents/skills/qr-recognition-integration/SKILL.md를
따른다.

PC 웹캠으로 QR을 인식해서 파싱 결과를 콘솔에 출력하는 임시 테스트 스크립트를
vision/qr_test.py에 만들어줘. cv2.QRCodeDetector를 사용하고, ATM에서 실제로 쓸
로직과 동일하게 만들어서 나중에 pi/main.py로 그대로 옮길 수 있게 해줘.
```

```powershell
# [터미널 3]
python vision/qr_test.py
```
**확인**: 2단계에서 만든 위험 QR을 스마트폰/태블릿 화면에 띄우고 노트북 웹캠에
비춰서, 콘솔에 `risk_level`이 올바르게 파싱되어 찍히는지 확인합니다.

### 3-2. 콜센터 확인 흐름 완성
```
너는 이 프로젝트의 backend-agent다. .agents/skills/callcenter-session-integration/SKILL.md를
따른다.

POST /api/v1/atm/scan으로 스캔 결과가 들어오면 analysis_sessions.atm_status를
갱신하고 WebSocket으로 대시보드에 브로드캐스트하는 로직을 추가해줘. DANGER로 판정된
세션은 atm_status를 CALL_CENTER로 전환하고, GET /api/v1/atm/session-status로
해결 여부(callcenter_resolution)를 확인할 수 있게 해줘. 아직 별도 상담원 화면이
없으니, 대시보드에 세션을 WITHDRAW_ENABLED로 되돌리는 관리자용 버튼도 임시로
추가해줘(스킬의 "MVP 대안" 참고).
```
**확인**: Swagger에서 `POST /api/v1/atm/scan`으로 위험 세션을 스캔했다고 보고하면
대시보드에 `WITHDRAW_BLOCKED`/`CALL_CENTER` 상태가 뜨고, 관리자용 버튼을 누르면
`WITHDRAW_ENABLED`로 바뀌는지 확인합니다.

---

## 4단계 (3주차). 하드웨어 마이그레이션 (Mock → 라즈베리파이 5, QR 인식 + 현금 배출 통합)

`.agents/workflows/hardware-swap.md`를 기본으로 따르되, 이 팀은 **QR 인식과 현금
배출 장치 제어가 같은 라즈베리파이 안에서 함께 동작**한다는 점이 다릅니다(3-1에서
만든 테스트 로직을 그대로 옮겨옵니다).

> 💡 이 단계를 완료한 뒤부터 ATM/하드웨어 관련 개발을 별도 담당자가 이어받는다면,
> 백엔드·DB 담당자와 서로의 코드를 매번 확인하지 않고도 협업할 수 있도록
> **`docs/백엔드-라즈베리파이5-연동-인터페이스-가이드.md`**도 함께 참고합니다(단,
> 이 팀은 desired-state를 항상 폴링하지는 않고 QR을 읽는 순간에만 로컬로 판단한다는
> 차이가 있습니다 — `qr-recognition-integration` 스킬 참고).

```
너는 이 프로젝트의 hardware-agent다. .agents/rules/hardware-rules.md와
.agents/skills/hardware-integration/SKILL.md, .agents/skills/qr-recognition-integration/SKILL.md를
따른다.

backend/iot/base.py의 DeviceProvider 인터페이스를 준수하는 HardwareDeviceProvider로
현금 배출 시연 장치를 gpiozero/lgpio로 제어해줘. vision/qr_test.py의 QR 인식 로직을
pi/main.py로 옮기고, QR을 읽는 즉시 로컬에서 SAFE/DANGER를 판단해 바로
HardwareDeviceProvider를 호출하도록 만들어줘. 판단 직후 POST /api/v1/atm/scan으로
백엔드에 보고하는 것도 포함해줘.
```

### 라즈베리파이 쪽 준비 (직접 실행)
라즈베리파이 5는 `gpiozero` 외에 RP1 칩 제어를 위한 `lgpio`가 필수이고, QR 인식을
위한 `opencv-python`도 필요합니다. 파이 터미널에서:
```bash
cd pi
python3 -m venv venv
source venv/bin/activate
pip install gpiozero lgpio opencv-python requests python-dotenv
```

Windows PC의 IP를 확인하여 파이에 알려줍니다:
```powershell
ipconfig
# IPv4 주소 확인 (예: 192.168.0.25)
```
그 값을 `pi/.env`의 `BACKEND_URL`에 적습니다:
```
BACKEND_URL=http://192.168.0.25:8000
```

배선은 **전원을 끈 상태**에서, **교사 입회 하**에 연결합니다. 연결 후:
```powershell
# backend/.env
DEVICE_MODE=hardware
```
**확인**: 위험 QR을 라즈베리파이 카메라에 비추면 현금 배출 시연 장치가 차단되고,
대시보드에 `WITHDRAW_BLOCKED`/`CALL_CENTER`가 실시간으로 반영되는지 확인합니다.
정상 QR(SAFE)에서는 출금 동작이 정상적으로 되는지도 함께 확인합니다.

---

## 5단계 (4주차). 클라우드 배포

`.agents/workflows/cloud-migration.md`를 그대로 따릅니다.

> ⚠️ 이 팀의 PRD는 "핵심 기능이 인터넷 연결 하나에만 의존하지 않을 것"을 요구사항으로
> 두고 있고, 8주 자체 일정에도 별도 클라우드 배포 단계가 없습니다. **이 단계를 이
> 팀에 적용할지는 교사·팀이 먼저 결정**해야 합니다 — 로컬 시연이 목표라면 5단계를
> 건너뛰고 4단계까지만 완성해도 됩니다.

```
너는 이 프로젝트의 db-agent다. .agents/skills/db-migration/SKILL.md를 따른다.

지금 MySQL 스키마를 Supabase로 마이그레이션할 SQL과 RLS 정책을 만들어줘.
```
```
너는 이 프로젝트의 deploy-agent다. .agents/skills/deployment/SKILL.md를 따른다.

Render/Vercel 배포에 필요한 설정 파일을 만들어줘.
```
Supabase 프로젝트 생성, Render/Vercel 배포는 각 사이트에서 직접 진행합니다.

**확인**: 배포된 Vercel URL로 접속해 문자 분석 → QR 생성 → ATM QR 인식 → 출금
제한/허용 → 콜센터 확인까지 전체 경로가 정상 동작하는지 확인합니다.

---

## 💡 토큰 80% 절약을 위한 핵심 실천 수칙
1. **사전 보일러플레이트 활용**: 이미 만들어진 기본 파일(`base.py`, `websocket_manager.py`, `ConnectionBadge.tsx`, `statusColor.ts`)을 다시 만들어달라고 하지 않습니다.
2. **Rules는 3개만 Always On**: `karpathy-principles`, `security-rules`, `coding-standards`만 켜둡니다.
3. **단계 전환 시 새 채팅**: 백엔드→프론트→하드웨어 단계로 넘어갈 때마다 우측 상단 `+` 버튼으로 새 대화를 시작합니다.
4. **학생 수작업 직접 실행**: venv 생성, 패키지 설치, DB 초기화, Git 커밋은 학생이 직접 터미널/GUI로 실행합니다.

---

## 막혔을 때 (FAQ & 트러블슈팅)

| 상황 / 에러 메시지 | 해결 방법 |
|---|---|
| `ModuleNotFoundError: No module named 'backend'` | `backend/main.py` 최상단에 `sys.path` 주입 코드가 포함되어 있는지 확인하거나, `cd backend` 경로에서 `uvicorn main:app`을 실행합니다. |
| `pip freeze` 후 `unsupported mime type charset=utf-16le` 에러 | PowerShell의 기본 리다이렉션 대신 `pip freeze \| Out-File -Encoding utf8 requirements.txt` 명령을 사용합니다. |
| `RuntimeError: The starlette.testclient module requires the httpx package` | 백엔드 가상환경에서 `pip install httpx`를 실행합니다 (`backend/requirements.txt`에 포함됨). |
| Next.js 대시보드 `Hydration failed` 경고 | 시간 렌더링 태그에 `suppressHydrationWarning` 속성을 추가하거나 `components/dashboard/ConnectionBadge.tsx`를 사용합니다. |
| `Control request failed: Failed to fetch` | 백엔드 서버(터미널 1: `http://localhost:8000`)가 켜져 있는지 확인합니다. |
| 웹캠 화면이 검은색으로 나오거나 멈춤 | Zoom, Teams, 브라우저 등 웹캠을 점유 중인 다른 프로그램을 모두 작업 관리자에서 종료합니다. |
| `Activate.ps1` 실행 불가 (`PSSecurityException`) | PowerShell에서 `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser` 실행 후 다시 활성화합니다. |
| `mysql: command not found` | `C:\xampp\mysql\bin\mysql.exe -u root`로 직접 지정하거나 phpMyAdmin 웹 UI SQL 탭에 붙여넣어 실행합니다. |
| QR이 잘 인식되지 않음(`detectAndDecode`가 빈 문자열 반환) | 화면 밝기를 높이고, QR을 화면 중앙에 카메라와 너무 가깝거나 멀지 않게(20~30cm) 비춥니다. 스마트폰 화면 반사가 심하면 각도를 살짝 기울입니다. |
| `pip install opencv-python`이 vision/pi 양쪽에서 중복되어 헷갈림 | 정상입니다 — 3단계는 PC(`vision/venv`)에서 미리 검증하는 것이고, 4단계는 실제 라즈베리파이(`pi/venv`)에 같은 패키지를 다시 설치하는 것입니다. 두 가상환경은 서로 다릅니다. |
| `analysis_sessions`에러/`session_id` 불일치로 `session-status` 조회 실패 | QR에 들어간 `session_id`와 스캔 보고(`POST /api/v1/atm/scan`)에 쓴 `session_id`가 정확히 같은 문자열인지 확인합니다. |
