# 라즈베리파이 5 ATM 데몬 의존성
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
opencv-python>=4.9.0
requests>=2.31.0
python-dotenv>=1.0.1
pydantic>=2.6.0

# 실기기(라즈베리파이 5)에서만 필요하다. Windows/PC 검증 단계에서는 설치하지 않아도 된다.
# Pi 5는 GPIO 칩이 달라(RP1) lgpio 핀 팩토리가 필요하다 — hardware-integration 스킬 참고
gpiozero>=2.0 ; platform_machine == "aarch64"
lgpio>=0.2.2.0 ; platform_machine == "aarch64"
